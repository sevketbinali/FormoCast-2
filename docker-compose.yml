version: '3.8'

services:
  formocast_app:
    build: .
    container_name: formocast_app
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    environment:
      - PYTHONUNBUFFERED=1
    command: uvicorn src.api.server:app --host 0.0.0.0 --port 8000 --reload
